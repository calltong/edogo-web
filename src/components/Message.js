import React from 'react'
import { observer, inject } from 'mobx-react'

export default class Message extends React.Component {
  render() {
    let text = ''//this.props.lang.getText(this.props.id)
    return text
  }
}
